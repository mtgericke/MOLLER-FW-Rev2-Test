#User Configuration

#OE_TERMINAL = "tmux"

IMAGE_BOOT_FILES_zynqmp = "rootfs.cpio.gz.u-boot boot.scr Image system.dtb"
SERIAL_CONSOLES_CHECK = "${SERIAL_CONSOLES}"
IMAGE_FEATURES = "allow-root-login"

UBOOT_EXTLINUX = "1"
UBOOT_EXTLINUX_ROOT = "root=/dev/ram0"

# UBOOT_EXTLINUX_FDT = "../${MACHINE}-system.dtb"
# UBOOT_EXTLINUX_CONSOLE = "console=ttyPS0,${baudrate}"

# UBOOT_EXTLINUX_KERNEL_ARGS_append = " earlycon clk_ignore_unused"